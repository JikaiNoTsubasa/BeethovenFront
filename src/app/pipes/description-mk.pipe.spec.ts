import { DescriptionMKPipe } from './description-mk.pipe';

describe('DescriptionMKPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionMKPipe();
    expect(pipe).toBeTruthy();
  });
});
